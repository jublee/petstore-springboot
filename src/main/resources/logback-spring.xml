<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- Import Spring Boot default console coloring and patterns -->
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>

    <!-- Define a property for the log directory -->
    <property name="LOG_DIR" value="./logs"/>

    <!-- 1. Console Appender -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>${CONSOLE_LOG_PATTERN}</pattern>
            <charset>utf8</charset>
        </encoder>
    </appender>

    <!-- 2. Daily Rolling File Appender -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_DIR}/app.log</file>

        <!-- Roll over daily and when the file reaches 10MB -->
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <!-- Save compressed history using .gz suffix -->
            <fileNamePattern>${LOG_DIR}/archived/app-%d{yyyy-MM-dd}.%i.log.gz</fileNamePattern>
            <!-- Max size per log file before rolling over -->
            <maxFileSize>10MB</maxFileSize>
            <!-- Keep 30 days of history total -->
            <maxHistory>30</maxHistory>
            <!-- Cap total size of all archives at 3GB -->
            <totalSizeCap>3GB</totalSizeCap>
        </rollingPolicy>

        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
            <charset>utf8</charset>
        </encoder>
    </appender>

    <!-- Package-level overrides -->
    <logger name="org.springframework.web" level="INFO"/>
    <logger name="com.yourpackage" level="DEBUG"/>

    <!-- Root logger rule -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <!-- <appender-ref ref="FILE"/> -->
    </root>

</configuration>